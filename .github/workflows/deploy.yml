name: Deploy to GCE

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 16 * * *' # 16:00 UTC is 00:00 Taipei Time (UTC+8)

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            set -e
            cd ~/ai_crypto_strategyOpenHands, 
根據 v37 的回測結果，雖然 MDD 有所改善，但勝率過低導致系統仍在失血。請立即執行 Iteration 38：

1. 提高進場嚴苛度：
   - 將 RSI 進場門檻從 42 降至 35。
   - 增加過濾條件：進場時的成交量必須低於過去 5 根 K 線的平均成交量 (代表拋壓枯竭)。

2. 重新校準 ATR 指標：
   - 止損設為 1.5 * ATR (更緊湊)。
   - 止盈設為 3.0 * ATR 或觸及布林上軌。

3. 調低 Spike Guard 門檻：
   - 將 atr_spike_factor 從 1.5 降至 1.2，加強亂流保護。

4. 再次運行回測：
   - 跑完後回報 NEAR 和 SOL 的 Win Rate 是否提升至 30% 以上。
            git fetch --all && git reset --hard origin/main

            printf "TELEGRAM_BOT_TOKEN=%s\n" "${{ secrets.TELEGRAM_BOT_TOKEN }}" > .env
            printf "TELEGRAM_CHAT_ID=%s\n" "${{ secrets.TELEGRAM_CHAT_ID }}" >> .env
            printf "GEMINI_API_KEY=%s\n" "${{ secrets.GEMINI_API_KEY }}" >> .env

            # 1. Ensure Node.js and NPM are installed
            if ! command -v npm &> /dev/null; then
              echo "NPM not found. Attempting installation..."
              sudo apt-get update && sudo apt-get install -y nodejs npm
            fi

            # 2. Local NPM Setup
            mkdir -p ~/.npm-global
            npm config set prefix '~/.npm-global'
            export PATH=$HOME/.npm-global/bin:$PATH

            # 3. Install PM2 locally if not present
            if [ ! -f "$HOME/.npm-global/bin/pm2" ]; then
              echo "Installing PM2..."
              npm install -g pm2
            fi

            PM2_BIN="$HOME/.npm-global/bin/pm2"

            pip3 install -r requirements.txt

            if [ "${{ github.event_name }}" == "schedule" ]; then
              echo "Running daily summary..."
              export PYTHONPATH=$PYTHONPATH:.
              python3 -m src.summary
            else
              echo "Deploying market monitor with PM2..."
              export PYTHONPATH=$PYTHONPATH:.

              # 4. PM2 Deployment Logic (Iteration 32: Physical Reset)
              $PM2_BIN delete all || true
              $PM2_BIN start src/market.py --name "Iteration31_Stable" --interpreter python3
              $PM2_BIN save

              echo "✅ PM2 Status Verification:"
              $PM2_BIN list > pm2_status.tmp
              cat pm2_status.tmp

              echo "🔍 Recent Trading Logs:"
              if [ -f "logs/trading.log" ]; then
                tail -n 20 logs/trading.log
              else
                echo "No trading.log found yet."
              fi

              echo "🔍 Process Verification:"
              ps aux | grep "src.market" | grep -v grep
            fi
