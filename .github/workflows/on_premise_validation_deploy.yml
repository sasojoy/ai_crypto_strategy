OpenHands, 
日誌顯示 NameError 發生在 `src/notifier.py` 的第 151 行。你只修了 market.py 是不夠的。
立即執行 Iteration 86.0 「跨文件語法清障」：

1. 修正 src/notifier.py (核心重點)：
   - 在 `src/notifier.py` 的最頂端立即加入：
     ```python
     import datetime
     from datetime import datetime, timedelta
     ```
   - 確保所有調用 `datetime.utcnow()` 或 `datetime.now()` 的地方都有定義。

2. 再次檢查 src/market.py：
   - 確保頂端也有完整的 `import datetime`。

3. 修正版本號與清潔：
   - 全域搜尋並移除所有 "Iteration 86.0" 或 "Iteration 86.0" 等過期字串（日誌裡還在噴！）。
   - 統一版本號為：🚀 【Iteration 86.0 | Final Stability Fix】。

4. 物理執行：
   - 使用 SSH_HOST/USER/KEY 部署，並執行 `pm2 restart all`。
   - 部署後，請你自己執行 `python3 -c "from src.notifier import send_rich_heartbeat; print('Import Test Passed')"` 確保模組能被正確載入。            cd $PROD_DIR
            
            # Iteration 85.1: Final Stability Fix Deployment
            pm2 stop all || true
            pm2 delete all || true
            rm -rf ~/staging_area/*
            pm2 start src/market.py --name "Iteration85_Live" --update-env
            pm2 save
            
            echo "✅ Deployment Successful!"



