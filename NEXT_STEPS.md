# 專案清理進度記錄 (2026-08-21)

這份筆記記錄本次「分析並清理專案」工作的進度，方便下次繼續（尚未加入 git 追蹤）。

## 已完成（皆已 commit 到 main）
1. ✅ `CHANGELOG.md` 損毀內容修復 — 原本被一段不相關的舊內容覆蓋（含殘留的 `EOF > changelog.md` heredoc 文字）。已還原並整併成一份完整版本。
2. ✅ 四份分散的 changelog（`CHANGELOG.md`、`changelog.md`、`docs/CHANGELOG.md`、`docs/CHANGELOG_AI.md`）整併成單一 `CHANGELOG.md`，並補齊先前漏記的 `v600.35`–`v600.46`。
   - 發現根本原因：Windows 檔案系統不分大小寫，`CHANGELOG.md` 與 `changelog.md` 其實是同一個實體檔案，導致內容互相污染。以後只維護大寫的 `CHANGELOG.md`。
3. ✅ `README.md` 標題版本號同步為 `v600.46`（原本卡在 `v600.12` 還帶一串重複後綴）。
4. ✅ `src/market.py` 與 `src/notifier.py` 的 `STRATEGY_VERSION` 統一為 `"[v600.46]"`（原本三個檔案三種版本號互不相同）。
5. ✅ 停用的「2.0 架構」（`strategy/main.py`、`strategy/logic.py`、`backtest/engine.py`、`realtime/binance_executor.py`、`data/fetcher.py`、`config/config.yaml`）已封存到 `archive/2.0-architecture/`，因為它已被 `src/market.py` 取代且無人維護（最後修改都停在 2026-04-11）。
6. ✅ 依附在該 2.0 架構上、已經壞掉的腳本（`optimizer.py`/`v2`/`v3`/`v4_robust`、`trace_optimizer.py`、`oos_stealth_validator.py`、`oos_validator.py`、`friction_test.py`、`final_audit_runner.py`）封存到 `archive/2.0-architecture/scripts/`。
7. ✅ 被取代的舊版腳本（`backtest_v72.py`/`v72_2`/`v73`、根目錄 `optimizer.py`、`run_backtest_iter18/19.py`、`update_evaluate.py`、`update_market.py`）封存到 `archive/legacy-scripts/`。
8. ✅ 停止追蹤執行期產生的大型輸出檔（`live_commando.log` 8MB、`trading.log` 918KB、`backtest_result.png`、`backtest_results_*.csv`、`audit_results.txt`、`pm2_status.txt`），刪除空的 `FF.txt`，並更新 `.gitignore` 避免再次被加入版控。

9. ✅ `src/market.py` 的 `fetch_1h_data()` 裸 `except: return pd.DataFrame()` 已改為 `except Exception:` 並印出 `traceback.print_exc()`，風格與 `run_strategy()` 的錯誤處理一致，符合 `docs/DEVOPS_RULES.md`「軍規八：禁止錯誤掩蓋」。（commit `59e4a77`）
10. ✅ `src/notifier.py` 的 `send_rich_heartbeat()`（及僅供它使用的 `get_progress_bar()`）已整個刪除 — 它引用 `json`/`pandas` 卻沒 import，且呼叫的 `market.get_recent_performance()`、`market.get_ai_filtered_count()` 在目前的 `src/market.py` 裡根本不存在，是壞掉又沒人呼叫的死代码。同步移除 `src/market.py` 中對它的無用 import。確認過整個專案（排除 `archive/`）沒有任何地方呼叫它。

最新 commit：`86b3044 CLEANUP: Archive dead scripts and stop tracking runtime output/log files`（第 9、10 項尚未 commit）

## 待處理
（目前無明確待辦項目，皆已完成；以下為可選項目，見下方）

## 順便發現、尚未處理的問題（可自行決定要不要修）
- `models/trainer.py`、`execution/live_dry_run.py` 仍然 import 已封存的 2.0 架構模組，但沒有全部搬過去（見 `archive/2.0-architecture/README.md` 的說明），需要再仔細看過才能決定去留。
- `config/config.yaml` 已封存（因為只有已停用的 2.0 架構在讀它，`src/market.py` 從未讀取），symbols/risk 參數目前仍是寫死在 `src/market.py` 裡。

## 另一條進行中的工作：驗證「這套系統到底能不能賺錢」（2026-08-30 開始）
這是跟上面清理工作分開的另一條線，目的是誠實回答「這套系統有沒有實際獲利能力」，過程中發現的東西：

1. ✅ 凍結的正式模型 `models/model_v118_xgb.joblib`（19 個技術指標 + 0.85 門檻）：用 2 年真實成本回測，PF 0.94，複利 -70.02%，季度間表現完全不穩定。腳本：`scripts/oos_backtest.py`。
2. ✅ 用同一組 19 特徵、乾淨走向式驗證（訓練/測試永遠分離、7 天禁運期防標籤洩漏）重新訓練：7 折 AUC 全部落在 0.48~0.54（等於隨機），PF 0.77，-41.36%。腳本：`scripts/walk_forward_backtest.py`。
3. ✅ 換訊號來源為 Binance 資金費率（未平倉量/多空比 API 只留 30 天歷史，抓不到 2 年，暫時不可行）：一樣的走向式驗證，7 折 AUC 落在 0.44~0.55，一樣是雜訊，PF 0.83，-12.67%（訊號更少，多季零訊號）。腳本：`scripts/fetch_funding_rates.py`、`scripts/walk_forward_funding.py`。

**三次獨立測試（換模型、換驗證法、換訊號來源）都測不出任何邊際優勢**，指向問題可能出在更根本的地方（1H 週期 + 這幾個主流幣 + 這個持倉規則下，價格方向本身就難預測），而不是單一特徵組或模型的問題。

**已與使用者約定的驗證規則（之後所有新嘗試都要遵守）：**
- **開發區**：2024-08-30 ~ 2025-12-31。之後所有新想法（新特徵、新標籤、新時間顆粒度等）都只能在這段資料上反覆迭代測試。
- **封存區**：2026-01-01 之後的資料完全不能碰，直到選出一個「這次真的有信心」的方案，才能在封存區做**一次、且只有一次**的最終驗證。沒通過就是否決，不能回頭調整再測第二次，否則封存區就失去意義、又會落入多重比較的過擬合陷阱。
- 原因：如果繼續拿同一批測試資料一次次換想法測、只留下「看起來有效」的那次，本質上跟這個專案自己批評過的 `src/oos_verify.py`（用測試集挑參數）是同一種錯誤，只是這次是人腦在做多重比較。

4. ✅ 日線雙 EMA(20/100) 順勢系統：表面 +1825%，但拆解後發現 68% 獲利集中在 3 筆交易（對到兩波史詩牛市），是牛市 beta 偽裝成訊號，非真訊號。
5. ✅ 費波那契/雙底/突破回踩/SNR 型態測試 + 控制組（任意比例都測）+ 500 次蒙地卡羅拆解，最後鎖定「EMA20 反彈確認」機制，撐過開發區所有拆解測試，動用唯一一次封存區驗證：**未通過**（贏隨機基準的幅度從開發區 100% 掉到封存區 56~86%）。整份結論寫在 `RESEARCH_FINDINGS.md`。

**研究已在 2026-09-02 收斂出結論並寫成 `RESEARCH_FINDINGS.md`：目前免費公開資料在這個時間尺度/幣種上找不到禁得起驗證的訊號，建議暫停實盤投入。**

## 2026-09-06：使用者要求繼續同方向研究，追加第九、十次測試
1. ✅ 資金費率極端值逆勢操作（模擬散戶心態、反向操作 + 日線多時間框架濾網）：兩個變體都明確失敗，複利表現甚至輸給 100% 的隨機進場基準（不只是「沒優勢」，是比隨機更差）。腳本：`scripts/dev_sentiment_fade.py`。
2. ✅ 布林通道壓縮突破：結果太弱不明確（只贏隨機基準 62~69%，PF≈1.00 打平），不足以判定為訊號，不建議繼續調參。腳本：`scripts/dev_vol_squeeze.py`。

3. ✅ 使用者追問「市場鎖定虛擬貨幣，模型應該要可以訓練吧」：把技術指標(5)+資金費率(6)+訂單流(6)=17個特徵**合併成同一個模型**訓練（之前每次都只單獨測一種來源），是還沒補過的測試。結果：4折平均 AUC 從單一來源最高 0.512 提升到 **0.527**，證明三種資料確實有一點互補資訊，但季度間不穩定（其中一季只觸發10筆訊號）、換算實際交易後 PF 0.91、複利 **-19.04%**，仍是虧錢的——小幅 AUC 提升不足以蓋過交易成本。腳本：`scripts/dev_combined_signals.py`。

詳細數字與結論已寫入 `RESEARCH_FINDINGS.md` 的「第九、十、十一次測試」章節。**依規則三者都不再回頭調整重測。**

## 2026-09-07：使用者問「專業交易員會怎麼獲利」、「能否組合弱訊號+嚴格風控」
1. ✅ 補跑之前寫好但沒報告過的 `scripts/dev_funding_carry.py`（delta-neutral 資金費率套利，不預測方向）：BTC/ETH/NEAR/AVAX 六年年化 +7.6%~+15.3%，SOL 太弱（+0.35%/yr）。這是目前唯一驗證出正期望值的機制。
2. ✅ 使用者選擇「組合式：多個弱訊號疊加+嚴格風控」，但先診斷出分散只能降波動、無法把負期望值變正，前11次測試裡除了套利全部≈0或負，不該硬疊加。改用「跨幣種資金費率套利投資組合 + 風控斷路器」（腳本：`scripts/dev_carry_portfolio.py`）：**分散到四幣反而拉低風險調整後報酬**（Sharpe-like 0.96→0.74，因為資金費率在去槓桿時共同轉負，不是真分散），風控斷路器能有效壓低尾部風險但贏不過單押BTC。

結論已寫入 `RESEARCH_FINDINGS.md`「第十二次分析」。

3. ✅ 使用者說「跑」→ 執行資金費率套利策略的封存區（2026-01至今，8個月）唯一一次最終驗證（腳本：`scripts/holdout_carry_validation.py`，程式碼與開發區完全不變）。**結果：通過。** BTC-only 封存區年化 +2.26%/yr（開發區+13.02%/yr）、四幣分散+風控版 +1.90%/yr（開發區+12.20%/yr），兩者封存區都仍為正報酬，只是幅度明顯縮水（主因開發區均值被2021年史詩牛市拉高；負資金費率佔比也從12.5%升到28.7%，顯示這個結構性優勢本身在變弱）。

**這是整個研究 13 次測試中，唯一真正撐過「開發區反覆測、封存區只驗一次」完整流程的策略。** 詳細數字已寫入 `RESEARCH_FINDINGS.md` 開頭摘要與「第十三次測試」章節，該文件的整體結論也已同步更新（方向預測類建議暫停實盤；資金費率套利可作為小規模、嚴格風控下的候選方案）。

## 2026-09-07（續）：使用者質疑固定 2xATR SL / 4xATR TP 太死板，追加第14、15次測試
1. ✅ 使用者指出固定R:R出場沒有動態調整、「被市場玩弄是必然」——批評成立但要分情況：ML類（AUC~0.50）不會被出場管理救回來（問題在進場沒方向資訊，且標籤本身綁定固定出場規則）；規則型的EMA20反彈值得重測，因為它是唯一在開發區用固定出場就有邊際優勢的候選人。
2. ✅ 做了 EMA20反彈 + Chandelier移動停損版（無固定停利、停損只上移不下移），腳本：`scripts/dev_bounce_trailing_exit.py`。開發區表現更好（複利+376% vs 原版+169%），但代價是集中度上升、贏隨機基準的優勢變弱（82%/80% vs 100%/100%）。
3. ✅ 使用者說「跑」→ 執行這個新版本的封存區唯一驗證（腳本：`scripts/holdout_trailing_exit_validation.py`）。**結果：更明確地沒通過**——只贏隨機基準28.8%(複利)/12.8%(PF)/44.4%(勝率)，比固定版的86.4%/56%/56%更差。分幣種看BTC/ETH仍賺錢但SOL/NEAR/AVAX全虧錢。

**結論：固定出場、動態出場兩個版本的EMA20反彈都已個別用掉唯一的封存區驗證機會，都沒通過。** 詳細數字已寫入 `RESEARCH_FINDINGS.md`「第十四、十五次測試」章節。

## 2026-09-07（續）：使用者要求打破「不再有第三次」的規矩，測第三個出場版本
1. ✅ 使用者說出場不該無腦、要用其他指標重新分析——確認後**明確同意打破規則**，約定：開發區先測、要贏過前兩版才進封存區、不論結果都是最後一版。
2. ✅ 開發區測試「EMA20反彈 + RSI/MACD指標確認出場」（硬停損2xATR全程有效，獲利達1xATR後才武裝智慧出場，RSI動能衰竭或MACD轉空任一觸發出場）：n=389，PF 1.52，複利+455.95%（三版最高），贏隨機基準100/86.4/80.0%。腳本：`scripts/dev_bounce_indicator_exit.py`。
3. ✅ 使用者說「跑」→ 封存區唯一驗證：n=37，PF 1.73，複利+23.89%（絕對報酬三版最高），但**贏隨機基準只有52.4/11.2/26.2%——三版最差，明確未通過**。腳本：`scripts/holdout_indicator_exit_validation.py`。

**關鍵發現：三個出場版本封存區都失敗，且呈現一致模式——2026這段期間行情異常平順上漲，隨機亂買都賺翻，而三種「更聰明」的出場邏輯本質上都是提早落袋，在這種持續緩漲的行情裡反而少賺。證明問題出在進場訊號本身（開發區六年市場結構的產物），與出場邏輯選擇無關。EMA20反彈三戰三敗，正式除名，不再測第四版。**

## 2026-09-07（續）：使用者要求繼續方向類預測，追加第18、19次測試
1. ✅ 相對強弱ML排名預測（腳本`scripts/dev_relative_strength_ml.py`）：AUC 0.504，換算市場中性多空交易PF 0.78、一年-69.58%。第5種預測目標定義收斂在AUC~0.50。
2. ✅ 換完全不同資料來源——總經連動（標普500/美元指數/黃金/VIX，透過`scripts/fetch_macro_data.py`用yfinance抓取，測試腳本`scripts/dev_macro_signals.py`）：AUC 0.509，全程0筆訊號觸發。第6種完全不同資料來源，一樣收斂在AUC~0.50。

**已建議使用者：19次測試、6種資料來源高度一致地指向「免費公開資料在這個時間尺度上沒有可預測方向的訊號」，繼續換資料源大概率不會有不同結果，除非投入付費資料源（鏈上、訂單簿深度、新聞情緒）。下一步待使用者決定是否繼續。**

## 2026-09-07（續）：使用者要求改成4小時主攻+15分鐘輔助確認 —— 進行中，下次從這裡繼續

**新抓的資料（已快取，下次不用重抓）：**
- `data/backtest_cache/*_15m_dev.csv`：5幣種15分鐘K棒，2020~2025開發區（透過 `scripts/fetch_15m_data.py`）
- `data/backtest_cache/macro_daily.csv`：標普500/DXY/黃金/VIX日線（透過 `scripts/fetch_macro_data.py`，第19次測試用）

**測試腳本：** `scripts/dev_mtf_4h_15m.py`（4H技術指標模型 + 15分鐘開盤區間突破確認進場，方法論細節見 `RESEARCH_FINDINGS.md`「第二十次測試」）。**目前腳本裡的 `DEV_TEST_QUARTERS` 已設定為2022Q1~2025Q4（4年版本，最新跑過的版本），可直接重跑不用改設定。**

**目前結果摘要：** 贏隨機基準（複利/PF/勝率）85.0/84.5/85.5%——是目前ML類方法裡表現最好的一個，但逐季訊號數暴衝暴停（0筆到524筆都有）、AUC在0.444~0.560間跳動、近半數季度AUC低於0.5，這是獨立於百分位數的穩定性警訊，跟EMA20反彈六年間逐年穩定的模式不同。

## 2026-09-08：MTF 4H+15分鐘策略的診斷結果 + 封存區驗證（第21次測試，已完成，未通過）

1. ✅ 診斷：把開發區 MTF-CONFIRMED 交易明細按年份、按幣種分組（腳本改在 `dev_mtf_4h_15m.py` 加上 by-year/by-symbol breakdown）。結果證實疑慮：**2023年（52%樣本）幾乎打平，獲利集中在2022+2025；ETH（-17.8%）幾乎抵銷BTC（+15.0%），NEAR佔樣本過半但貢獻薄弱，SOL僅4筆卻貢獻異常大。** 告知使用者後，使用者明確選擇仍送封存區做唯一驗證。
2. ✅ 封存區驗證（腳本：`scripts/holdout_mtf_validation.py`，規則與開發區完全相同，訓練方式改成用全部開發區資料一次性訓練單一模型）：補抓2026年至今的15分鐘資料（`scripts/fetch_15m_holdout.py`）與更新到最新的1H資料。**結果：AUC 0.542，138個原始訊號→冷卻後24筆→確認後僅11筆。MTF-CONFIRMED：n=11，PF 1.22，複利+2.39%，但贏隨機基準只有複利65.0%/PF 68.0%/勝率49.5%——勝率退化到等同丟硬幣，比開發區的85.5%大幅倒退。且24筆原始訊號中22筆（91.7%）是NEAR，ETH/SOL/AVAX全部掛零，完全印證診斷階段的疑慮。判定：未通過。**

**這是MTF策略研究線的最終結果，第21次測試，第5個用掉封存區驗證額度卻失敗的候選人。** 詳細數字已寫入 `RESEARCH_FINDINGS.md`「第二十一次測試」章節，該文件開頭摘要與建議也已同步更新。

**目前狀態：研究的核心問題已有明確答案。21次測試中，方向預測類方法（含固定/動態出場、多時間框架等各種變體）全部驗證失敗；資金費率套利是唯一通過完整驗證的策略。** 下一步待使用者決定：(a) 到此為止，考慮資金費率套利的小規模實際部署（需要額外的保證金/清算/交易所風險設計，這份研究沒做）、(b) 評估付費資料源找新的方向性訊號、(c) 換市場/資產/時間尺度、(d) 其他。

## 2026-09-08（續）：使用者提出「量能確認方向/預測幅度」新想法，第22次測試

1. ✅ 基底訊號選一個全新、跟先前封存區額度無關的簡單技術觸發點：RSI(14)超賣/超買交叉（1H線，6年5幣種，8根K棒冷卻）。腳本：`scripts/dev_volume_confirm.py`。
2. ✅ 基準線（不加量能過濾）本身就是負期望值：n=5263，勝率31.2%，PF 0.82，複利趨近-100%——再次印證「這個時間尺度極端讀數傾向動能延續、非回歸均值」（呼應第九次資金費率測試）。
3. ✅ H1（量能確認方向）：不成立且方向相反——量能越大逆勢單輸得越慘（Spearman rho=-0.164, p<0.001），高量能子集輸給100%隨機基準。
4. ✅ H2（量能預測波動幅度）：不成立，三個量能分位組後續波動幅度幾乎相同，相關係數不顯著（rho=0.026, p=0.062）。

**意外發現：量能與逆勢單損益有統計顯著（p<0.001）的負相關，暗示量能可能是動能延續訊號、不是逆勢反轉的確認訊號。** 這指向一個真正還沒測過的新假說（RSI極值+高量能=順勢進場，不同於第九次測試過的兩個逆勢方向）。詳細數字見 `RESEARCH_FINDINGS.md`「第二十二次測試」。

## 2026-09-08（續）：第23次測試——RSI+量能動能延續，目前診斷品質最好的候選人，已鎖定正式規格

1. ✅ 使用者要求繼續測，做了順勢（動能延續）版本：RSI(14)跌破30放空/突破70做多。腳本：`scripts/dev_momentum_continuation.py`。加量能前三分位篩選後：n=1754，勝率40.4%，PF 1.20，贏過100%的500次隨機抽樣基準。
2. ✅ 集中度診斷（逐年/逐幣種/前3筆集中度）：全部通過，逐年全正、跨幣種穩定（僅BTC微幅打平）、前3筆集中度僅0.4%——是全研究最乾淨的分佈。
3. ✅ 逐季穩定度（24季）：18季PF>1.0，訊號數平滑成長（29→118），沒有MTF模型那種暴衝暴停。
4. ✅ 參數穩健性控制組（腳本：`scripts/dev_momentum_control.py`，5組RSI門檻×4種量能篩選比例=20格網格）：結果平滑單調（越極端參數效果越強），跟費波那契0.618「換任意比例都一樣」的破綻模式相反，是真實機制該有的樣子。
5. ✅ 使用者明確決定：**鎖定 RSI(14)跌破30放空/突破70做多 + 量能前三分位（觸發母體內計算）+ SL=2xATR/TP=4xATR/168根1H棒(7天)持倉上限/8根K棒冷卻 為正式候選規格**（30/70+前三分位是控制組網格跑之前就定好的原始參數，不是網格裡數字最好的那格，避免用結果選參數）。

**已如實揭露的限制：** 複利數字用依序複利演算法計算，但這個策略持倉可重疊（冷卻僅8小時、持倉上限7天），複利百分比會虛高，未來評估實際部署需換算成固定名目部位。

## 2026-09-08（續）：多空拆分穩健性檢查 + 第24次測試（封存區驗證，通過）

1. ✅ 使用者要求優先做「多空分開的穩健度」檢查：發現明顯不對稱——超買→做多六年逐年全正、五幣種全正（PF 1.31），超賣→做空六年裡三年負報酬（2020/2021/2023，剛好是最大兩波牛市）、BTC淨虧損（PF 1.11，較弱）。使用者明確決定**不因此調整已鎖定的多空合併規格**，維持原規格送封存區。
2. ✅ 封存區驗證（腳本：`scripts/holdout_momentum_validation.py`，規則不變，RSI/ATR/量能比在連續1H資料上計算讓冷卻/暖身狀態自然跨過邊界，只統計2026-01-01後觸發的交易）：**n=214，勝率37.4%，PF 1.03，複利+0.18%。對照隨機基準：複利83.0%/PF 83.0%/勝率84.4%——三項一致偏高，沒有任何一項崩到接近50%。判定：通過，但邊際優勢很小。** 細節印證多空不對稱在封存區同樣重現（做多PF 1.07/+4.94%，做空PF 1.00/-4.53%）。

**這是研究裡第二個、也是第一個方向性策略通過完整的開發區反覆測+封存區單次驗證流程**（另一個是資金費率套利，非方向性）。詳細數字見 `RESEARCH_FINDINGS.md`「額外診斷」「第二十四次測試」，該文件開頭摘要與建議也已同步更新。

**目前狀態：研究的核心問題已有更完整的答案。** 24次測試中，除RSI動能延續（做多側尤其穩健）與資金費率套利外，其餘方向預測類方法全部驗證失敗。下一步待使用者決定：(a) 到此為止，評估這兩個通過驗證的策略如何小規模實際部署（保證金/清算/交易所風險、RSI策略的持倉重疊複利膨脹問題都還沒設計）、(b) 繼續找新方向性訊號、(c) 投入付費資料源、(d) 其他。

## 2026-09-08（續）：部署風險評估報告 + 模擬盤監控程式（已完成，尚未排程）

1. ✅ 寫了 `DEPLOYMENT_RISK_ASSESSMENT.md`，針對資金費率套利與RSI動能延續兩個通過驗證的策略評估小規模部署的風險（保證金/清算、雙腳執行、交易所集中風險、持倉重疊複利膨脹、多空不對稱、相關性風險）。
2. ✅ 用固定名目部位＋同時持倉上限重新計算RSI策略的真實報酬（腳本：`scripts/dev_momentum_fixed_notional.py`），發現原本的複利數字在開發區灌水約10倍、在封存區反而低估——封存區真實年化應為+13~31%/yr而非原本算出的+0.27%/yr。已更新進 `DEPLOYMENT_RISK_ASSESSMENT.md` 與 `RESEARCH_FINDINGS.md`。
3. ✅ 使用者選擇先建立「模擬盤+通知」（不接真實下單API權限）。已建立 `paper_trading/` 目錄：
   - `calibrate_momentum_threshold.py`：從2020至今全部歷史觸發點計算量能前三分位門檻，寫入 `thresholds.json`（已跑過一次，門檻2.71）。
   - `momentum_monitor.py`：即時抓取5幣種1H K棒，用鎖定規格判斷RSI+量能觸發，用固定名目部位（2%風險/筆）+ 同時持倉上限5筆模擬交易，透過 `src/notifier.py` 的Telegram通知進出場。
   - `funding_monitor.py`：即時追蹤BTC/ETH資金費率trailing 3日均，用鎖定規格判斷持有/空手狀態，狀態轉換時Telegram通知。
   - 兩者都已測試跑過、修好首次執行bug（原本會把回溯期內的舊訊號當成新訊號誤判）、確認第二次執行行為正常。
   - **尚未做：** 設定Windows工作排程器讓它們定期自動執行（`paper_trading/README.md`已寫好PowerShell指令範例）、設定`.env`裡的Telegram token/chat ID（目前沒設，程式會正常運作但不會真的發訊息，只印在console）。

**下一步待使用者決定：** 要不要現在就設定`.env`和工作排程器讓模擬盤開始真正定期自動執行？或是先手動跑幾次確認邏輯符合預期？

以上所有回測腳本與快取資料（`data/backtest_cache/`、`scripts/*_trades.csv`）都還沒加入 git 追蹤，是研究用的暫存產出。
