
# 🛡️ 五百萬計畫：最高運維軍規 (DEVOPS_RULES)

為了確保 AI 策略系統在無人值守環境下的極致穩定性，任何接手本專案的 AI 必須嚴格遵守以下六大軍規：

### 【軍規一：實地驗證制 (On-Premise Validation)】
*   **核心要求**：禁止在 GitHub Actions 虛擬環境執行回測。所有驗證必須在生產環境 (GCE) 的 `~/staging_area` 進行。
*   **執行標準**：GitHub Actions 僅作為觸發器。部署流程必須包含：`SSH -> Staging -> Backtest -> Gate Check -> Production Sync`。只有當 GCE 實地回測達標 (WR>60%)，才允許覆蓋正式目錄。

### 【軍規二：環境凍結】
*   **核心要求**：禁止在常規部署流程中頻繁執行 `pip install`。
*   **執行標準**：環境變更（如升級 `scikit-learn`）必須透過獨立的 `setup_env.sh` 執行。常規 `deploy.sh` 應僅負責代碼同步與進程重啟。

### 【軍規三：單次加載原則】
*   **核心要求**：模型加載 (`ml_model.load()`) 與 API 初始化嚴禁進入 `while True` 主循環。
*   **執行標準**：所有重量級資源必須在 `if __name__ == "__main__":` 區塊中初始化一次，並以引用方式傳遞給策略函數。

### 【軍規四：數據邊界硬檢查】
*   **核心要求**：針對交易所 API 可能回傳的異常值（如成交量突降 -97%）實施硬性 Clip 限制。
*   **執行標準**：若檢測到數據異常（如跌幅 > 80%），系統必須強制將其歸零或使用緩存值，並發出 `⚠️ Data Anomaly` 警告，嚴禁因數據缺失導致錯誤交易。

### 【軍規五：版本追蹤日誌】
*   **核心要求**：每次代碼變更必須同步更新 `docs/CHANGELOG_AI.md`。
*   **執行標準**：紀錄必須包含：觸發原因、具體修改內容、預期回測數據或觀察指標。

### 【軍規六：README 上帝視角】
*   **核心要求**：`README.md` 必須包含「快速診斷手冊」與「核心策略邏輯」。
*   **執行標準**：確保新進 AI 能在 30 秒內理解當前策略門檻（如 AI Score > 0.72）與核心風控邏輯。

### 【軍規七：語法完整性檢查】
*   **核心要求**：所有涉及 `datetime` 或新 Library 的改動，必須在本地執行 `python3 -c "import ..."` 驗證成功後方可提交。
*   **執行標準**：嚴禁在生產環境噴出 NameError。提交前必須確保所有依賴與變數已正確導入。

### 【軍規八：禁止錯誤掩蓋】
*   **核心要求**：嚴禁在 `try-except` 中使用 `return 0.5` 或預設值掩蓋 AI 預測錯誤。
*   **執行標準**：所有異常必須透過 `logger.error(e)` 拋出真實 Traceback，系統必須保持 100% 透明，讓錯誤直接顯現以利診斷。

### 【軍規九：版本號強對齊】
*   **核心要求**：`STRATEGY_VERSION` 必須在 `src/market.py` 與 `src/notifier.py` 同步更新。
*   **執行標準**：嚴禁版本號分裂。每次迭代必須確保所有核心文件的版本標籤完全一致。

---
*本文件為 AI 策略系統之遺產，嚴禁未經授權之刪除。*
